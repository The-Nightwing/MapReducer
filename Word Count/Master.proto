syntax = "proto3";
package rpc;

message Request {
    string status = 1;
}

message Response {
}

service Master {
    rpc mapperFinished(Request) returns (Response);
    rpc reducerFinished(Request) returns (Response);
}