syntax = "proto3";
package rpc;

message Request {
    string index = 1;
}

message Response {
    
}

service MasterService {
    rpc MasterMap(Request) returns (Response) {};
    rpc MasterReducer(Request) returns (Response) {};
}